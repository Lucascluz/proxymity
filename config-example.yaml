proxy:
  host: "0.0.0.0"
  port: "8080"
  admin_port: "9090"  # Admin server for /health and /status

backend:
  - name: "dummy-1"
    url: "http://localhost:8081"
    health: "/health"  # Health check endpoint path
    weight: 3  # For weighted load balancing (higher = more traffic)
    enabled: true

  - name: "dummy-2"
    url: "http://localhost:8082"
    health: "/health"
    weight: 2
    enabled: true

  - name: "dummy-3"
    url: "http://localhost:8083"
    health: "/api/health"  # Custom health endpoint
    weight: 1
    enabled: true

  - name: "dummy-4"
    url: "http://localhost:8084"
    health: "/health"
    weight: 1
    enabled: false  # Disabled backend (won't receive traffic)

load-balancer:
  method: "round-robin"  # Options: "round-robin", "random", "least-connections", "weighted"

health-check:
  interval: 5  # Check backends every 5 seconds
  timeout: 3    # Health check request timeout in seconds
